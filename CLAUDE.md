# AI 内容创作智能化平台 - 开发规范 (V3.1)

## 🏗️ 核心实践策略 (13 条)

### 一、并行提效机制

1. **多进程并发**: 本地5个终端 + 网页端5-10任务同步运行
   - 关键技术: `&` 命令实现本地→网页任务迁移
   - 关键技术: `--teleport` 跨端会话无缝切换
   - 关键技术: iOS 碎片化任务预执行

2. **模型选型**: 全流程采用 Opus 4.5+ 思考模式
   - **优势对比**:
     | 维度 | Opus 4.5 | Sonnet |
     |------|----------|--------|
     | 代码理解深度 | 逻辑推理 | 基础语法 |
     | 工具调用成功率 | 98% | 83% |
     | 返工率 | ≤5% | ≥20% |

### 二、知识沉淀体系

3. **CLAUDE.md 中枢**: 实时记录错误案例，Git 版本控制
   - PR 审查自动更新规则（@claude 触发）
   - 标准化 Bun/NPM 工作流
   - 典型应用: 错误案例库、最佳实践沉淀

4. **自动化资产库**: `.claude/commands/` 斜杠命令仓库
   - 流程自动化（如 `/commit-push-pr` 含内联 bash 脚本）
   - 自定义快捷命令

5. **专项子智能体**:
   - `code-simplifier`: 代码精简与优化
   - `verify-app`: 端到端测试

### 三、质量保障闭环

6. **三重验证设计**:
   - 即时校验: PostToolUse 钩子自动格式化
   - 深度验证: AgentStop 回归测试
   - 终极保障: ralph-wiggum 插件沙盒验证

7. **安全防护策略**: `/permissions` 白名单替代 `--dangerously-skip-permissions`

8. **MCP 协议集成**: 深度对接 Slack/Sentry/BigQuery

### 四、核心心法

9. **计划优先模式**:
   - 双 Shift + Tab 进入深度方案设计
   - 执行阶段编辑通过率提升至 92% 以上

10. **验证闭环法则**:
    - 自动化测试框架（Chrome 插件自测）
    - **关键指标**:
      - 代码质量提升 300%
      - 返工时间减少 80%

11. **语义一致性**: 严格遵循项目技术栈（React 19, Tailwind, Lucide）

12. **代码完整性**: SearchReplace 严禁截断函数头或闭合结构

13. **验证先行 (Manual Verification First)**: 针对复杂前端页面（如 TikTok Studio），严禁盲猜逻辑。必须先提供 F12 Console 调试指令供用户手工验证，确认选择器与事件 100% 有效后再编写代码。

### Case 005: Superficial Audit & False Completion
- **现象**: 在深度审核中宣布系统 "87% 完成"，但实际上 `npm run test` 脚本失效，且核心自动化函数 `handleCommentAction` 仅为 TODO 占位。
- **根因**: 审计深度不足，过度依赖文件存在性/架构完整性，而非实际的闭环执行。
- **修复**: 补全 Vitest 环境，将所有 TODO 占位实现为功能代码，并统一全系统类型定义。
- **预防**: 每次审计必须包含「闭环验证」：1. 执行 Build/Test/Type-check；2. `grep` 搜索项目内所有 TODO/FIXME；3. 端到端追踪关键数据链路（源头→中继→目标）。

---

## 🛠️ 标准化工作流
- **现象**: 在深度审核中宣布系统 "87% 完成"，但实际上 `npm run test` 脚本失效，且核心自动化函数 `handleCommentAction` 仅为 TODO 占位。
- **根因**: 审计深度不足，过度依赖文件存在性/架构完整性，而非实际的闭环执行。
- **修复**: 补全 Vitest 环境，将所有 TODO 占位实现为功能代码，并统一全系统类型定义。
- **预防**: 每次审计必须包含「闭环验证」：1. 执行 Build/Test/Type-check；2. `grep` 搜索项目内所有 TODO/FIXME；3. 端到端追踪关键数据链路（源头→中继→目标）。

## 🛠️ 标准化工作流

### 黄金功能保护系统

> ### ⚖️ 最高宪法
> **[`GOLDEN_FUNCTIONS_CONSTITUTION.md`](./GOLDEN_FUNCTIONS_CONSTITUTION.md)** 定义了 **8 大闭环 × 31 个黄金功能**。
> 这是本项目开发的**最高准则**，任何代码修改都必须以此为底线。
> 修改前必须声明涉及的黄金功能编号，修改后逐一验证 — **无例外**。

**核心原则**: 绝不破坏现有工作代码

#### 8 大功能闭环（详见宪法文档）

| 闭环 | 核心功能 | 黄金功能编号 | 关键守护文件 |
|------|---------|-------------|------------|
| ① 🎬 视频生成与上传 | GeminiGen + Veo3 API + Google Flow + Google Vids → 自动上传 | #1a-#6 | content.js, background.js, autoPilot.js, workflow.js, veoService.ts |
| ② 📊 Analytics 数据收集 | Ask Studio AI + DOM 抓取 + YPP 采集 | #7-#12 | youtube-analytics.js |
| ③ 🧠 DFL 动态反馈 | 自动触发 → 病毒检测 → 内容生成 | #13-#17 | dflLearningService.ts, YouTubeAnalytics.tsx |
| ④ 📝 计划生成 | Ask Studio 计划 + 数据适配 + 验证 | #18-#20 | askStudioAdapter.ts |
| ⑤ 💬 评论自动化 | 定时评论 + Ignite 马甲 + DFL 自动评论 | #21-#24 | commentAutomation.js |
| ⑥ 🌐 跨平台分发 | YouTube + TikTok + X/Twitter | #25-#27 | background.js |
| ⑦ 🔧 系统基础设施 | 健康检查 + ACK + 双向消息桥 | #28-#30 | content.js (**#30 是全系统心脏**) |
| ⑧ 🎨 React 前端 | API 认证 + 视频生成 + Dashboard + 多语言 | #31 | App.tsx, YouTubeAnalytics.tsx |

#### 快照与恢复
```bash
# 修改前创建快照
npm run snapshot create "Before modifying background.js"

# 列出所有快照
npm run snapshot list

# 对比当前状态与快照
npm run snapshot compare

# 恢复最新快照（遇到 bug 时）
npm run snapshot restore
```

#### 修改前验证
```bash
# 1. 声明涉及的黄金功能编号 (参考宪法文档)
# "本次修改涉及黄金功能 #___, #___, #___"

# 2. 创建快照
npm run snapshot create "Pre-refactor checkpoint"

# 3. 运行端到端测试
npm run verify:e2e workflow "video generation"

# 4. 运行类型检查
npm run type-check

# 5. 运行 linting
npm run lint
```

#### 回归检查清单
修改任何黄金功能后必须验证：

```bash
# 1. 运行黄金功能验证
npm run verify:golden full

# 2. 运行端到端测试
npm run verify:e2e all

# 3. 类型检查
npm run type-check

# 4. Lint 检查
npm run lint

# 5. 对比快照确认未破坏逻辑
npm run snapshot compare
```

#### 错误防御编程规范
所有处理外部数据的代码必须添加防御：

```typescript
// ✅ 正确：添加防御检查
if (!data || !data.videoUrl) {
  console.error('Invalid data received:', data);
  return;
}

// ❌ 错误：直接使用可能导致崩溃
const videoUrl = data.videoUrl; // 可能为 undefined
```

#### EXT_CONSTANTS 使用规则
修改涉及以下 API 前必须检查 EXT_CONSTANTS：

```typescript
// constants.ts
export const EXT_CONSTANTS = {
  CRITICAL_FILES: ['background.js', 'content.js', 'services/geminiService.ts'],
  PROTECTED_APIS: ['chrome.storage', 'chrome.runtime', 'chrome.tabs'],
  MESSAGE_TYPES: ['VIDEO_READY', 'UPLOAD_START', 'ANALYTICS_UPDATE']
} as const;
```

### 常规开发
| 命令 | 用途 |
|------|------|
| `npm run dev` | 启动开发服务器 |
| `npm run type-check` | 类型检查 |
| `npm run test` | 执行测试 |
| `npm run snapshot create [desc]` | 创建快照 |
| `npm run snapshot restore` | 恢复快照 |
| `npm run verify:golden full` | 验证黄金功能 |
| `npm run verify:e2e all` | 端到端测试 |

### 质量验证
- **即时诊断**: `GetDiagnostics`
- **预览验证**: `OpenPreview`
- **黄金功能检查**: `npm run verify:golden full`
- **端到端测试**: `npm run verify:e2e all`

### 🚀 增强的 Skills 系统 (V3.2)

#### 第一阶段：极高优先级（已实现）

| Skill | 文件 | 功能 | 命令 | 状态 |
|--------|------|------|-------|--------|
| 性能监控 | `services/performanceMonitorService.ts` | 实时性能监控 | `performance-monitor:*` | ✅ 已实现 |
| 错误追踪 | `services/errorTrackingService.ts` | 错误收集和分析 | `error-tracking:*` | ✅ 已实现 |
| 安全扫描 | `services/securityScanService.ts` | 安全漏洞扫描 | `security-scan:*` | ✅ 已实现 |

#### 第二阶段：高优先级（已实现）

| Skill | 文件 | 功能 | 命令 | 状态 |
|--------|------|------|-------|--------|
| 代码审查 | `services/codeReviewService.ts` | 自动代码审查 | `code-review:*` | ✅ 已实现 |
| 自动化部署 | `scripts/deploy.js` | 多环境部署 | `deploy:*` | ✅ 已实现 |

#### 第三阶段：中优先级（已实现）

| Skill | 文件 | 功能 | 命令 | 状态 |
|--------|------|------|-------|--------|
| 配置管理 | `services/configManagerService.ts` | 多环境配置 | `config:*` | ✅ 已实现 |
| 依赖管理 | `services/dependencyManagerService.ts` | 依赖扫描和管理 | `dependencies:*` | ✅ 已实现 |

#### 第四阶段：低优先级（待实现）

| Skill | 文件 | 功能 | 状态 | 状态 |
|--------|------|------|-------|--------|
| 自动化测试 | - 测试覆盖率扩展 | `test:*` | ✅ 已实现 |

---

## 🛠️ 标准化工作流
1. 提供 F12 调试指令
2. 用户手工验证
3. 基于反馈编写代码
4. 确认选择器与事件 100% 有效

### 影响分析规则
修改任何代码前必须明确说明影响范围：

```
[修改前分析]
涉及文件: background.js, content.js
修改类型: chrome.storage 逻辑
影响范围: 所有使用存储的组件
风险评估: 高

[验证计划]
1. 创建快照
2. 验证黄金功能
3. 运行端到端测试
4. 对比快照确认无回归

[回滚计划]
如发现问题，执行：npm run snapshot restore
```

---

*文档版本: V3.1 | 最后更新: 2026-01-13*
