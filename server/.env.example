# OAuth Server Configuration
# 复制此文件为 .env 并填入您的 Google OAuth 凭据

# Google OAuth2 凭据
# 从 https://console.cloud.google.com/apis/credentials 获取
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# OAuth 回调 URI
# 必须与 Google Console 中配置的 "Authorized redirect URIs" 完全一致
# 注意：这是后端服务器的端口，不是前端端口
GOOGLE_REDIRECT_URI=http://localhost:51122/oauth-callback

# 后端服务器端口
OAUTH_SERVER_PORT=51122

# 前端应用 URL（用于 CORS 配置）
FRONTEND_URL=http://localhost:4000

# 前端 OAuth 回调 URL（用于将令牌返回给前端）
FRONTEND_OAUTH_CALLBACK=http://localhost:4000/oauth-callback
