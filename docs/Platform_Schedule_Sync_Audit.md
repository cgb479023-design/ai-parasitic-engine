# 四平台视频发布调度同步审核报告

## 📅 审核日期
2026-01-07

## 🎯 审核目标
确保所有 4 个视频生成平台的发布调度数据正确同步：
1. React 发布计划中的时间 → 视频生成平台 → YouTube Studio → 最终状态

---

## 📊 数据流追踪

### 通用数据结构

React 计划项 (`item`):
```javascript
{
    title: "Video Title",
    description: "...",
    tags: ["tag1", "tag2"],
    publishTimeLocal: "01/07/2026 09:00 AM",   // 完整时间字符串
    scheduleDate: "01/07/2026",                 // 日期部分 (MM/DD/YYYY)
    scheduleTime: "9:00 AM",                    // 时间部分 (HH:MM AM/PM)
    pinnedComment: "First comment..."
}
```

---

## 🔵 平台 1: GeminiGen

### 数据流
```
┌─────────────────────────────────────────────────────────────────────┐
│ 1. React processVideo() 接收 item                                    │
│    Line 8644-8670                                                   │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 2. 打开 GeminiGen 标签页生成视频                                      │
│    window.postMessage({ type: 'OPEN_GEMINIGEN_TAB', url })          │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 3. GeminiGen autoPilot.js 生成视频后发送 GEMINI_VIDEO_RESULT         │
│    payload: base64VideoData                                         │
│    ⚠️ 注意: 此消息不包含 uploadData                                  │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 4. React 接收视频后在本地构建 uploadPayload (Line 8788-8799)         │
│                                                                     │
│    const uploadPayload = {                                          │
│        id: index,                                                   │
│        title: finalTitle,                                           │
│        scheduleDate: scheduleDate,  ✅ 从 item 解析                  │
│        scheduleTime: scheduleTime,  ✅ 从 item 解析                  │
│        ...                                                          │
│    }                                                                │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 5. React 发送 PREPARE_YOUTUBE_UPLOAD + OPEN_YOUTUBE_UPLOAD_TAB      │
│    Line 8801-8807                                                   │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 6. studioUploader.js 设置 Schedule                                   │
│    使用 scheduler.setSchedule(data.scheduleDate, timeValue)         │
│    Line 836-838                                                     │
└─────────────────────────────────────────────────────────────────────┘

✅ 结论: GeminiGen 调度数据正确同步 (React 本地处理)
```

---

## 🟢 平台 2: Google Vids

### 数据流
```
┌─────────────────────────────────────────────────────────────────────┐
│ 1. React processVideo() 发送 GOOGLE_VIDS_GENERATE                    │
│    Line 8579-8594                                                   │
│                                                                     │
│    uploadData: {                                                    │
│        title, description, tags,                                    │
│        scheduleTime: item.publishTimeLocal,  ✅ 完整时间字符串       │
│        scheduleDate: item.scheduleDate,      ✅ 日期 MM/DD/YYYY     │
│        scheduleTimeOnly: item.scheduleTime,  ✅ 时间 HH:MM AM/PM    │
│        videoIndex, pinnedComment                                    │
│    }                                                                │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 2. reactBridge.js 转发到 background.js                               │
│    action: 'storeGoogleVidsRequest'                                 │
│    Line 209-223                                                     │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 3. background.js 存储请求数据                                        │
│    googleVidsState.pendingRequest = { prompt, aspectRatio, uploadData }│
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 4. workflow.js 生成视频后发送到 background                           │
│    Line 592-601, 621-630                                            │
│                                                                     │
│    uploadData: {                                                    │
│        scheduleDate: uploadData?.scheduleDate || '',  ✅            │
│        scheduleTime: uploadData?.scheduleTime || '',  ✅            │
│        scheduleTimeOnly: uploadData?.scheduleTimeOnly || '',  ✅    │
│        ...                                                          │
│    }                                                                │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 5. background.js 存储视频数据并打开 YouTube Studio                    │
│    Line 144-156                                                     │
│                                                                     │
│    videoData = {                                                    │
│        scheduleDate: uploadData?.scheduleDate || '',  ✅            │
│        scheduleTime: uploadData?.scheduleTime || '',  ✅            │
│        scheduleTimeOnly: uploadData?.scheduleTimeOnly || '',  ✅    │
│    }                                                                │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 6. studioUploader.js 设置 Schedule                                   │
│    使用 scheduler.setSchedule(data.scheduleDate, timeValue)         │
└─────────────────────────────────────────────────────────────────────┘

✅ 结论: Google Vids 调度数据正确同步
```

---

## 🟠 平台 3: Google Flow

### 数据流
```
┌─────────────────────────────────────────────────────────────────────┐
│ 1. React processVideo() 发送 GOOGLE_FLOW_GENERATE                    │
│    Line 8579-8594 (与 Google Vids 相同的 uploadData)                 │
│                                                                     │
│    uploadData: {                                                    │
│        scheduleTime: item.publishTimeLocal,  ✅                     │
│        scheduleDate: item.scheduleDate,      ✅                     │
│        scheduleTimeOnly: item.scheduleTime,  ✅                     │
│        ...                                                          │
│    }                                                                │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 2. reactBridge.js 转发到 background.js                               │
│    action: 'storeGoogleVidsRequest' (共用存储)                       │
│    Line 226-245                                                     │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 3. autoPilot.js 生成视频后发送到 background                          │
│    Line 444-448, 491-494                                            │
│                                                                     │
│    uploadData: {                                                    │
│        scheduleDate: uploadData.scheduleDate || '',  ✅             │
│        scheduleTime: uploadData.scheduleTime || '',  ✅             │
│        scheduleTimeOnly: uploadData.scheduleTimeOnly || '',  ✅     │
│        ...                                                          │
│    }                                                                │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 4. background.js 存储视频数据并打开 YouTube Studio                    │
│    Line 144-156                                                     │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 5. studioUploader.js 设置 Schedule                                   │
└─────────────────────────────────────────────────────────────────────┘

✅ 结论: Google Flow 调度数据正确同步
```

---

## 🔴 平台 4: Veo API Direct

### 数据流
```
┌─────────────────────────────────────────────────────────────────────┐
│ 1. React processVideo() 调用 veoService.generateVideoWithVeo()       │
│    Line 8526-8558                                                   │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 2. 视频生成完成后，React 本地构建 uploadPayload                       │
│    Line 8788-8799                                                   │
│                                                                     │
│    const uploadPayload = {                                          │
│        scheduleDate: scheduleDate,  ✅ 从 item 解析                  │
│        scheduleTime: scheduleTime,  ✅ 从 item 解析                  │
│        ...                                                          │
│    }                                                                │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 3. React 发送 PREPARE_YOUTUBE_UPLOAD + OPEN_YOUTUBE_UPLOAD_TAB      │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 4. studioUploader.js 设置 Schedule                                   │
└─────────────────────────────────────────────────────────────────────┘

✅ 结论: Veo API 调度数据正确同步 (React 本地处理)
```

---

## 📋 studioUploader.js 核心调度逻辑审核

### 调度数据接收 (Line 127-130)
```javascript
console.log("📅 [Studio Uploader] Schedule Data:", {
    scheduleDate: data.scheduleDate,
    scheduleTime: data.scheduleTime
});
```

### 调度模式检测 (Line 747-750)
```javascript
if (data.scheduleDate && data.scheduleTime) {
    console.log("📅 [Studio Uploader] Schedule Mode detected:");
    console.log("   📆 scheduleDate:", data.scheduleDate);
    console.log("   ⏰ scheduleTime:", data.scheduleTime);
```

### 调用 Scheduler (Line 836-838)
```javascript
console.log(`📅 [Studio Uploader] Calling scheduler with date="${data.scheduleDate}", time="${timeValue}"`);
const result = await window.YouTubeScheduler.setSchedule(data.scheduleDate, timeValue);
```

### Scheduler.js 执行 (Line 38-51)
```javascript
async setSchedule(scheduleDate, scheduleTime) {
    console.log(`📅 [Scheduler] Setting schedule: ${scheduleDate} ${scheduleTime}`);
    
    // ... 点击 Schedule 单选按钮 ...
    
    if (scheduleDate) {
        await this.setDate(scheduleDate);
    }
    
    if (scheduleTime) {
        await this.setTime(scheduleTime);
    }
}
```

---

## ✅ 审核结论

| 平台 | 调度数据传递 | Schedule 设置 | 状态同步 |
|------|-------------|---------------|----------|
| GeminiGen | ✅ React 本地处理 | ✅ 正确 | ✅ 预期工作 |
| Google Vids | ✅ uploadData 完整 | ✅ 正确 | ✅ 预期工作 |
| Google Flow | ✅ uploadData 完整 | ✅ 正确 | ✅ 预期工作 |
| Veo API | ✅ React 本地处理 | ✅ 正确 | ✅ 预期工作 |

---

## 🔧 确认状态同步的关键代码

### studioUploader.js 完成信号 (Line 1293-1297)
```javascript
chrome.runtime.sendMessage({
    action: 'relayYouTubeUploadComplete',
    videoUrl: videoUrl,
    videoId: finalVideoId,
    dbId: data.id,
    status: data.scheduleDate ? 'scheduled' : 'completed',  // ✅ 正确判断
    pinnedComment: null,
    scheduleTime: data.scheduleTime ? `${data.scheduleDate} ${data.scheduleTime}` : null
});
```

### background.js 中继完成信号
将 `YOUTUBE_UPLOAD_COMPLETE` 消息发送到 React 应用。

### React 更新计划状态
收到完成信号后更新 `yppPlan.schedule[index].publishedUrl` 和状态。

---

## 📅 文档版本
- **创建日期**: 2026-01-07
- **作者**: AI Content Creation Platform Development Team
